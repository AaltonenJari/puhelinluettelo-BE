{
  "name": "puhelinluettelo-be",
  "version": "1.0.0",
  "description": "puhelinluettelo back end",
  "repository": {
    "type": "git",
    "url": "bohelinluettelo-BE"
  },
  "license": "ISC",
  "author": "JA",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rimraf build && npm --prefix ../puhelinluettelo install && npm --prefix ../puhelinluettelo run build && npx cpy \"../puhelinluettelo/build/**\" \"build\" --parents",
    "deploy:full": "npm run build:ui && git add build -f && git commit -m \"Update UI build\" && git push",
    "lint": "eslint ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dataenv": "^1.0.0-beta.1",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "mongoose": "^9.0.1",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@stylistic/eslint-plugin": "^5.6.1",
    "cpy-cli": "^6.0.0",
    "eslint": "^9.39.2",
    "globals": "^16.5.0",
    "rimraf": "^6.1.2"
  }
}
